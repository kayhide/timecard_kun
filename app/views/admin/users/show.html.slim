- content_for :breadcrumb do
  ol.breadcrumb
    li= link_to t('helpers.links.home'), :root
    li= link_to User.model_name.human, [:admin, :users]
    li.active= @user

= render 'admin/month_selector', path: [:admin, @user], active_date: @cour.first

.panel.panel-default
  .panel-heading
    span=l @cour.first.to_date
    span 〜
    span=l @cour.last.to_date - 1.day
  = render 'admin/records/table', records: @records
